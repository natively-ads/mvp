
// this is where a publisher can view one of their networks with a networkId


const Page = ({ params }: { params: { networkId: string } }) => {
  return (
    <div>Page</div>
  )
}

export default Page